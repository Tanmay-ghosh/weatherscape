import 'package:flutter/material.dart';

void main()=>runApp(MyApp());


class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      //todo:2 Have to decide a theme for the app.
      home: Scaffold(),//todo:1 add a new dart page for the starting screen
    );
  }
}
